export class event_closure{
    private events = {};

    public add_event_listen(event:string, mothed:any){
        this.events[event] = mothed;
    }

    public call_event(event:string, argvs:any){
        if (this.events[event]){
            this.events[event].apply(null, argvs);
        }
    }
}